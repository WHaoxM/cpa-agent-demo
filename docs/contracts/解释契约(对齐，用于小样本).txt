{
  "schema_version": "2.7.5",
  "explanation_output": {
    "overall_logic": {
      "applied_path": "enum [STANDARD, FRESHMAN_PATH, PIVOTER_PATH]",
      "calibration_confidence": "float [0-1] (基于符合预测算法)",
      "is_degraded": "boolean (是否触发了vector_only降级)"
    },
    "dimension_breakdown": [
      {
        "dimension": "enum [basic, skills, literacy, potential]",
        "dimension_score": "float [0-1]",
        "adjusted_weight": "float (展示Softmax后的最终权重)",
        "key_evidence": [
          {
            "student_fact": "string (学生简历中的事实点)",
            "jd_requirement": "string (对应的JD原文子串)",
            "match_status": "enum [FULL, PARTIAL, GAP]",
            "reasoning": "string (Agent简短推导)"
          }
        ]
      }
    ],
    "graph_bridge": {
      "enabled": "boolean (仅在TIER_3或PIVOTER_PATH触发)",
      "shortest_path": ["list[string] (ESCO节点路径)"],
      "transition_difficulty": "number [0-1]",
      "bridge_skills": ["string (推荐补齐的中间技能)"]
    },
    "actionable_guidance": {
      "top_gap": "string (最核心的缺口说明)",
      "immediate_action": "string (建议针对该岗位的简历优化动作)"
    }
  }
}
